spring:
  application:
    name: bytesMenu

  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/saraiva?createDatabaseIfNotExist=true
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      database-platform: org.hibernate.dialect.MySQLDialect
      ddl-auto: update
